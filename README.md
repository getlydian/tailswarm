# tailswarm

A Docker Swarm controller that attaches Tailscale sidecars to labeled services
and registers them with a self-hosted Headscale control plane.

A complete README will land once the implementation catches up.
